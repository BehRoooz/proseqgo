# requirements.txt for the CAFA-5 MLOps solution

# mlflow 2.13 imports pkg_resources, removed in setuptools>=82 (Feb 2026)
setuptools<82

# torch >=2.0: install separately in GPU Dockerfiles (cu132 index) or locally as needed
numpy>=1.24
pandas>=2.0
tqdm>=4.65
torchmetrics>=1.0
pyyaml>=6.0
matplotlib>=3.7
transformers
safetensors

fastapi>=0.135.2
fastapi-cli>=0.0.24
fastapi-cloud-cli>=0.15.1
prometheus-client>=0.22.1
uvicorn>=0.42.0
python-multipart>=0.0.22

# Async job queue (Redis/RQ) + durable job store (Postgres)
redis>=5.0
rq>=2.0
psycopg[binary]>=3.1

mlflow==2.13.0
boto3>=1.34